# Windows防犯カメラ開発計画

## プロジェクト概要
Windows上で動作する、USBカメラを用いた防犯カメラ機能を開発する。
技術スタックはPython 3.14.6 / uv / pytest（詳細は[CLAUDE.md](CLAUDE.md)を参照）。

## 開発フローの全体方針
- 開発は以下の6フェーズに従って進める。
- 原則は順次進行（要件定義→設計→実装→テスト→レビュー→ドキュメント作成）とするが、
  **テスト・レビューで問題が発覚した場合は、該当する設計・実装フェーズに戻って修正する反復的なフローとする。**
- 各フェーズの終了時には、そのフェーズの完了条件を満たしていることを確認してから次フェーズへ進む。

```
要件定義 → 設計 → 実装 → テスト → レビュー → ドキュメント作成
              ↑________________|（NGの場合は設計/実装に戻る）
```

## フェーズ詳細

### 1. 要件定義
ユーザーの要求から、どのような機能を作成するか検討・合意するフェーズ。

検討項目:
- **機能要件**: 録画の開始/停止、モーション検知、検知時の通知方法、ライブビュー表示、
  録画データの閲覧方法、複数カメラへの対応有無
- **非機能要件**: 録画データの保存期間とストレージ管理（古い録画の自動削除/ローテーション）、
  長時間稼働時の安定性、CPU/メモリ使用率の制約、起動方法（手動/自動起動・常駐）
- **対象機器**: USB接続のWebカメラを前提とする
- **プライバシー・運用面**: 録画データの保存場所・アクセス制御方針

完了条件: 機能要件・非機能要件が一覧化され、ユーザーと合意できていること。

**進捗: 完了**。ユーザーへのヒアリングを経て機能要件・非機能要件を合意し、
[docs/requirements.md](docs/requirements.md) に要件定義書としてまとめた。

### 2. 設計
要件定義で合意した機能を、どのような方法で実装するか検討・決定するフェーズ。

検討項目:
- **技術選定**: カメラ制御・画像処理ライブラリの選定とその理由
- **アーキテクチャ**: モジュール構成（カメラ入力/検知処理/録画保存/通知/UI操作）
- **データ設計**: 録画ファイル形式、保存先ディレクトリ構成
- **異常系設計**: カメラ切断時、ディスク容量不足時などの挙動
- **セキュリティ設計**: 録画データへのアクセス制御方針

完了条件: 設計内容がドキュメント化され、要件定義の各項目を満たす設計になっていること。

**進捗: 完了**。技術選定・アーキテクチャ・データ設計・異常系・将来の通知モジュールの
拡張ポイントを含めて [docs/design.md](docs/design.md) にまとめ、ユーザーの承認を得た。

### 3. 実装
設計に基づき、実際にコーディングを行うフェーズ。

方針:
- ディレクトリ構成は`src/`に従う
- パッケージ追加は`uv add`を使用し、`pip install`は使用しない
- 設計で定めたモジュール単位で実装を進める

完了条件: 設計通りに主要機能が動作する状態になっていること。

**進捗: 完了**。`uv init`でプロジェクトを初期化し、`opencv-python`を追加。
設計書の構成通り `src/webcam_security/` 配下に `config.py / camera.py /
motion_detector.py / recorder.py / live_view.py / storage_cleaner.py / main.py`
を実装し、既定の `config.toml` を作成した。全モジュールのimport確認済み。
実機（USBカメラ）での動作確認は次フェーズ（テスト）で行う。

### 4. テスト
実装されたものが要件定義の要件を満たしているか、設計通り実装できているかをテストするフェーズ。

内容:
- **単体テスト**: pytestによるモジュール単位のテスト（`uv run pytest`で実行）。
  正常系・異常系・境界値を含める
- **結合テスト**: 検知→録画→保存→通知の連携動作確認
- **実機テスト**: 実際のUSBカメラを接続した動作確認（検知精度、録画品質、長時間稼働）
- **異常系テスト**: カメラ切断、ディスク容量不足等のエラーハンドリング確認

完了条件: 要件定義の機能・非機能要件を満たし、テストが全てパスしていること。
不合格の場合は実装または設計フェーズに戻って修正する。

**進捗: 完了**。単体テスト32件全てパス、ユーザー環境での実機テスト（2026-06-25実施）も
全項目パス。詳細は[docs/test_report.md](docs/test_report.md)。

### 5. レビュー
テスト完了したコードをレビューして、改善点やリスクなどをドキュメントにまとめるフェーズ。

観点:
- バグ・正確性、セキュリティリスク、パフォーマンス
  （`python-code-reviewer`エージェントの観点を活用）
- 改善点・リスクをレビュー結果としてドキュメント化

完了条件: 致命的な問題が解消されていること。軽微な改善点は対応事項として記録し、
必要に応じて実装フェーズに戻る。

**進捗: 完了**。`python-code-reviewer`エージェントによるレビューを実施し、
[docs/review.md](docs/review.md)にまとめた。致命的な問題はなし。重要な問題3点
（`storage_cleaner.py`の削除失敗時の例外処理、`recorder.py`の書き込み失敗検知の実効性、
`config.py`のbool値検証すり抜け）と軽微な改善提案5点が見つかったが、ユーザー判断により
**今回は修正せず、いずれ対応する事項として記録した上でレビューを完了**とする。

### 6. ドキュメント作成
テスト・レビューの結果を受けて、コード内にdocstringを記載したり、
README.mdに防犯カメラの操作方法などを記載するフェーズ。

内容:
- コード内docstring（Google Style、日本語、`doc-writer`エージェントを活用）
- README.md: プロジェクト概要、セットアップ手順（`uv sync`）、操作方法、
  ディレクトリ構成、トラブルシューティング

完了条件: README記載内容に従って第三者がセットアップ・操作できることを確認できる状態であること。

## リスク
- モーション検知の誤検知/検知漏れ
- 長時間録画によるディスク容量の圧迫
- USBカメラ機種ごとの互換性問題
- 録画データの取り扱いに関するプライバシー上の懸念

## 次のステップ
フェーズ1（要件定義）から着手し、具体的な機能要件をユーザーと合意する。
